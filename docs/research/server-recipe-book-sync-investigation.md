# Server recipe book — investigação

`ServerRecipeBook` envia estado conhecido/destacado no login e em updates.
Uma troca de recipes exige política explícita para preservar esses conjuntos;
nenhuma implementação foi adicionada.
