# ADR-019 — escopo de registries de tags

Status: aceito.

A categoria pública é `TAGS`. O provider agrupa internamente paths estáticos
conhecidos, registra dynamic/custom como suporte limitado e reporta desconhecidos.
Function tags são `FUNCTION_TAG_DOMAIN` e pertencem a functions. Nenhum registry
é bindado.

