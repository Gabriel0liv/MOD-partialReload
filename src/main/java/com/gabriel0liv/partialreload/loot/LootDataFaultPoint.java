package com.gabriel0liv.partialreload.loot;

public enum LootDataFaultPoint {
    BEFORE_PUBLICATION,
    AFTER_ELEMENTS_PUBLICATION,
    AFTER_TYPE_INDEX_PUBLICATION,
    DURING_VERIFICATION,
    DURING_ROLLBACK
}
