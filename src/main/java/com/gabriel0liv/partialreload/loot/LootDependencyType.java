package com.gabriel0liv.partialreload.loot;

public enum LootDependencyType {
    LOOT_TABLE_REFERENCE,
    PREDICATE_REFERENCE,
    ITEM_MODIFIER_REFERENCE,
    NESTED_CONDITION,
    NESTED_FUNCTION,
    COMPOSITE_ENTRY,
    DYNAMIC_DROP_REFERENCE
}

